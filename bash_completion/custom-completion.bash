#!/usr/bin/env bash
# globalprotect
complete -W "collect-log import-certificate launch-ui 'launch-ui [--recover]' 'show --version'" globalprotect
# xclip
complete -W "-i -in -o -out -f -filter -l -loop -t -target -d -display -h -help -selection -version -silent -quiet -verbose -noutf8" xclip
